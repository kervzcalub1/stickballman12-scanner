node_modules
dist
.vercel
.DS_Store

# Playwright E2E artifacts
/test-results/
/playwright-report/
/blob-report/
/playwright/.cache/

# Local Claude Code settings (may embed secrets)
.claude/settings.local.json

# Reference doc with plaintext credentials — never commit
api.rtf

# Local env & secrets — never commit (DB creds, API keys, tokens, etc.)
.env
.env.*
*.env
vercel.env
env.env
!.env.example
*.log

# Service-account / key material
*.pem
*.key
# Local HTTPS certs for phone testing (scripts/local-cert.mjs) — private keys
certs/
*-service-account*.json
gcp-*.json
credentials*.json

#version
version-update.txt
version-4.md

june22-progress.md
# Auto-downloaded ONNX cutout model (fetched on first use)
assets/branding/models/

# Recorded SOP tutorials — hosted on R2, never committed (see scripts/upload-sop-videos.mjs).
# ~20 MB of mp4/webm that changes independently of the code; videos.json holds the URLs.
sop-videos/

# Local intake/scratch notes — may hold prod credentials or API keys. NEVER commit.
# (Root .md files were scrubbed from history once already; keep them out.)
*.local.md

node_modules
dist
.vercel
.DS_Store

# Playwright E2E artifacts
/test-results/
/playwright-report/
/blob-report/
/playwright/.cache/

# Local Claude Code settings (may embed secrets)
.claude/settings.local.json

# Reference doc with plaintext credentials — never commit
api.rtf

# Local env & secrets — never commit (DB creds, API keys, tokens, etc.)
.env
.env.*
*.env
vercel.env
env.env
!.env.example
*.log

# Service-account / key material
*.pem
*.key
*-service-account*.json
gcp-*.json
credentials*.json

#version
version-update.txt
version-4.md

june22-progress.md